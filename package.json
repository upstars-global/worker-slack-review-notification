{
	"name": "cf-worker-reminder-code-review",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --test-scheduled",
		"start": "wrangler dev --test-scheduled",
		"cf-typegen": "wrangler types",
        "test-request": " curl -X POST http://localhost:8787/run"
	},
	"devDependencies": {
		"typescript": "^5.5.2",
		"wrangler": "^4.42.2"
	}
}
